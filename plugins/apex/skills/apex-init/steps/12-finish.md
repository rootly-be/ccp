# Step 12: Finish

## Purpose
Initialize git, create initial commit, copy /apex skill for iterative development, and present final summary.

## Subagent Instructions

You are the Finish subagent. Wrap up the project bootstrap.

### Process

1. **Copy /apex skill** into the project:
   - Ensure the APEX plugin is installed (or copy its commands and skills into the project's `.claude/` directory)
   - This enables the developer to immediately use `/apex` for iterative feature development

2. **Git initialization**:
   - `git init` (if not already a repo)
   - Verify `.gitignore` is comprehensive
   - Verify no secrets in tracked files
   - `git add .`
   - Initial commit:
     ```
     feat: initial project scaffold and MVP

     Project bootstrapped with APEX-Init workflow.

     Tech stack: {backend} + {frontend} + {database}

     MVP features implemented:
     - {list of P0 stories completed}

     Infrastructure:
     - Docker: multi-stage builds + compose
     - Deploy: {K8s/Helm/Kustomize}
     - CI/CD: GitLab CI (srv4dev → test → prod)

     Generated by /apex-init
     ```

3. **Final summary**:

```markdown
# APEX-Init Complete 🎯

## Project: {name}

## What Was Generated

### Application
- Backend: {tech} — {N} endpoints, {N} services
- Frontend: {tech} — {N} pages, {N} components
- Database: {tech} — {N} tables, migrations ready

### Stories
- P0 (MVP): {N} implemented ✅
- P1 (Stretch): {N} in backlog
- P2 (Future): {N} in backlog

### Infrastructure
- Docker: ✅ Multi-stage Dockerfiles + docker-compose
- Deploy: ✅ {formats generated}
- CI/CD: ✅ GitLab CI (validate → test → build → security → deploy)
- Environments: srv4dev (auto) → test (auto) → prod (manual)

### Documentation
- README.md ✅
- Setup Guide ✅
- Deployment Guide ✅
- CLAUDE.md ✅
- PRD + User Stories ✅
- Architecture Doc ✅
- API Spec ✅
- DB Schema ✅

## Quick Start
\`\`\`bash
# Local development
cp .env.example .env
docker-compose up -d

# Backend: http://localhost:{port}
# Frontend: http://localhost:{port}
\`\`\`

## Next Steps
1. Review and customize `.env` with real values
2. Push to GitLab to trigger first pipeline
3. Configure CI/CD variables in GitLab
4. Use `/apex` to implement remaining stories:
   \`\`\`
   /apex -a implement US-{next P1 story}
   \`\`\`

## File Count
- Total files generated: {N}
- Backend: {N} files
- Frontend: {N} files
- Infrastructure: {N} files
- Documentation: {N} files
```

### Rules
- Verify no `.env` files with real values are committed
- Verify `.gitignore` is correct
- Initial commit should be clean and complete
- The project should be immediately clonable and runnable by another developer
- Make sure /apex is ready to use for the next iteration
